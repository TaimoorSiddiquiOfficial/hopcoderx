---
title: CLI
description: HopCoderX CLI options and commands.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

The HopCoderX CLI by default starts the [TUI](/docs/tui) when run without any arguments.

```bash
HopCoderX
```

But it also accepts commands as documented on this page. This allows you to interact with HopCoderX programmatically.

```bash
HopCoderX run "Explain how closures work in JavaScript"
```

---

### tui

Start the HopCoderX terminal user interface.

```bash
HopCoderX [project]
```

#### Flags

| Flag         | Short | Description                                                             |
| ------------ | ----- | ----------------------------------------------------------------------- |
| `--continue` | `-c`  | Continue the last session                                               |
| `--session`  | `-s`  | Session ID to continue                                                  |
| `--fork`     |       | Fork the session when continuing (use with `--continue` or `--session`) |
| `--prompt`   |       | Prompt to use                                                           |
| `--model`    | `-m`  | Model to use in the form of provider/model                              |
| `--agent`    |       | Agent to use                                                            |
| `--port`     |       | Port to listen on                                                       |
| `--hostname` |       | Hostname to listen on                                                   |

---

## Commands

The HopCoderX CLI also has the following commands.

---

### agent

Manage agents for HopCoderX.

```bash
HopCoderX agent [command]
```

---

### attach

Attach a terminal to an already running HopCoderX backend server started via `serve` or `web` commands.

```bash
HopCoderX attach [url]
```

This allows using the TUI with a remote HopCoderX backend. For example:

```bash
# Start the backend server for web/mobile access
HopCoderX web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
HopCoderX attach http://10.20.30.40:4096
```

#### Flags

| Flag        | Short | Description                       |
| ----------- | ----- | --------------------------------- |
| `--dir`     |       | Working directory to start TUI in |
| `--session` | `-s`  | Session ID to continue            |

---

#### create

Create a new agent with custom configuration.

```bash
HopCoderX agent create
```

This command will guide you through creating a new agent with a custom system prompt and tool configuration.

---

#### list

List all available agents.

```bash
HopCoderX agent list
```

---

### auth

Command to manage credentials and login for providers.

```bash
HopCoderX auth [command]
```

---

#### login

HopCoderX is powered by the provider list at [Models.dev](https://models.dev), so you can use `HopCoderX auth login` to configure API keys for any provider you'd like to use. This is stored in `~/.local/share/HopCoderX/auth.json`.

```bash
HopCoderX auth login
```

When HopCoderX starts up it loads the providers from the credentials file. And if there are any keys defined in your environments or a `.env` file in your project.

---

#### list

Lists all the authenticated providers as stored in the credentials file.

```bash
HopCoderX auth list
```

Or the short version.

```bash
HopCoderX auth ls
```

---

#### logout

Logs you out of a provider by clearing it from the credentials file.

```bash
HopCoderX auth logout
```

---

### github

Manage the GitHub agent for repository automation.

```bash
HopCoderX github [command]
```

---

#### install

Install the GitHub agent in your repository.

```bash
HopCoderX github install
```

This sets up the necessary GitHub Actions workflow and guides you through the configuration process. [Learn more](/docs/github).

---

#### run

Run the GitHub agent. This is typically used in GitHub Actions.

```bash
HopCoderX github run
```

##### Flags

| Flag      | Description                            |
| --------- | -------------------------------------- |
| `--event` | GitHub mock event to run the agent for |
| `--token` | GitHub personal access token           |

---

### mcp

Manage Model Context Protocol servers.

```bash
HopCoderX mcp [command]
```

---

#### add

Add an MCP server to your configuration.

```bash
HopCoderX mcp add
```

This command will guide you through adding either a local or remote MCP server.

---

#### list

List all configured MCP servers and their connection status.

```bash
HopCoderX mcp list
```

Or use the short version.

```bash
HopCoderX mcp ls
```

---

#### auth

Authenticate with an OAuth-enabled MCP server.

```bash
HopCoderX mcp auth [name]
```

If you don't provide a server name, you'll be prompted to select from available OAuth-capable servers.

You can also list OAuth-capable servers and their authentication status.

```bash
HopCoderX mcp auth list
```

Or use the short version.

```bash
HopCoderX mcp auth ls
```

---

#### logout

Remove OAuth credentials for an MCP server.

```bash
HopCoderX mcp logout [name]
```

---

#### debug

Debug OAuth connection issues for an MCP server.

```bash
HopCoderX mcp debug <name>
```

---

### models

List all available models from configured providers.

```bash
HopCoderX models [provider]
```

This command displays all models available across your configured providers in the format `provider/model`.

This is useful for figuring out the exact model name to use in [your config](/docs/config/).

You can optionally pass a provider ID to filter models by that provider.

```bash
HopCoderX models anthropic
```

#### Flags

| Flag        | Description                                                  |
| ----------- | ------------------------------------------------------------ |
| `--refresh` | Refresh the models cache from models.dev                     |
| `--verbose` | Use more verbose model output (includes metadata like costs) |

Use the `--refresh` flag to update the cached model list. This is useful when new models have been added to a provider and you want to see them in HopCoderX.

```bash
HopCoderX models --refresh
```

---

### run

Run HopCoderX in non-interactive mode by passing a prompt directly.

```bash
HopCoderX run [message..]
```

This is useful for scripting, automation, or when you want a quick answer without launching the full TUI. For example.

```bash "HopCoderX run"
HopCoderX run Explain the use of context in Go
```

You can also attach to a running `HopCoderX serve` instance to avoid MCP server cold boot times on every run:

```bash
# Start a headless server in one terminal
HopCoderX serve

# In another terminal, run commands that attach to it
HopCoderX run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flags

| Flag         | Short | Description                                                             |
| ------------ | ----- | ----------------------------------------------------------------------- |
| `--command`  |       | The command to run, use message for args                                |
| `--continue` | `-c`  | Continue the last session                                               |
| `--session`  | `-s`  | Session ID to continue                                                  |
| `--fork`     |       | Fork the session when continuing (use with `--continue` or `--session`) |
| `--share`    |       | Share the session                                                       |
| `--model`    | `-m`  | Model to use in the form of provider/model                              |
| `--agent`    |       | Agent to use                                                            |
| `--file`     | `-f`  | File(s) to attach to message                                            |
| `--format`   |       | Format: default (formatted) or json (raw JSON events)                   |
| `--title`    |       | Title for the session (uses truncated prompt if no value provided)      |
| `--attach`   |       | Attach to a running HopCoderX server (e.g., http://localhost:4096)       |
| `--port`     |       | Port for the local server (defaults to random port)                     |

---

### serve

Start a headless HopCoderX server for API access. Check out the [server docs](/docs/server) for the full HTTP interface.

```bash
HopCoderX serve
```

This starts an HTTP server that provides API access to HopCoderX functionality without the TUI interface. Set `HOPCODERX_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `HopCoderX`).

#### Flags

| Flag         | Description                                |
| ------------ | ------------------------------------------ |
| `--port`     | Port to listen on                          |
| `--hostname` | Hostname to listen on                      |
| `--mdns`     | Enable mDNS discovery                      |
| `--cors`     | Additional browser origin(s) to allow CORS |

---

### session

Manage HopCoderX sessions.

```bash
HopCoderX session [command]
```

---

#### list

List all HopCoderX sessions.

```bash
HopCoderX session list
```

##### Flags

| Flag          | Short | Description                          |
| ------------- | ----- | ------------------------------------ |
| `--max-count` | `-n`  | Limit to N most recent sessions      |
| `--format`    |       | Output format: table or json (table) |

---

### stats

Show token usage and cost statistics for your HopCoderX sessions.

```bash
HopCoderX stats
```

#### Flags

| Flag        | Description                                                                 |
| ----------- | --------------------------------------------------------------------------- |
| `--days`    | Show stats for the last N days (all time)                                   |
| `--tools`   | Number of tools to show (all)                                               |
| `--models`  | Show model usage breakdown (hidden by default). Pass a number to show top N |
| `--project` | Filter by project (all projects, empty string: current project)             |

---

### export

Export session data as JSON.

```bash
HopCoderX export [sessionID]
```

If you don't provide a session ID, you'll be prompted to select from available sessions.

---

### import

Import session data from a JSON file or HopCoderX share URL.

```bash
HopCoderX import <file>
```

You can import from a local file or an HopCoderX share URL.

```bash
HopCoderX import session.json
HopCoderX import https://opncd.ai/s/abc123
```

---

### web

Start a headless HopCoderX server with a web interface.

```bash
HopCoderX web
```

This starts an HTTP server and opens a web browser to access HopCoderX through a web interface. Set `HOPCODERX_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `HopCoderX`).

#### Flags

| Flag         | Description                                |
| ------------ | ------------------------------------------ |
| `--port`     | Port to listen on                          |
| `--hostname` | Hostname to listen on                      |
| `--mdns`     | Enable mDNS discovery                      |
| `--cors`     | Additional browser origin(s) to allow CORS |

---

### acp

Start an ACP (Agent Client Protocol) server.

```bash
HopCoderX acp
```

This command starts an ACP server that communicates via stdin/stdout using nd-JSON.

#### Flags

| Flag         | Description           |
| ------------ | --------------------- |
| `--cwd`      | Working directory     |
| `--port`     | Port to listen on     |
| `--hostname` | Hostname to listen on |

---

### uninstall

Uninstall HopCoderX and remove all related files.

```bash
HopCoderX uninstall
```

#### Flags

| Flag            | Short | Description                                 |
| --------------- | ----- | ------------------------------------------- |
| `--keep-config` | `-c`  | Keep configuration files                    |
| `--keep-data`   | `-d`  | Keep session data and snapshots             |
| `--dry-run`     |       | Show what would be removed without removing |
| `--force`       | `-f`  | Skip confirmation prompts                   |

---

### upgrade

Updates HopCoderX to the latest version or a specific version.

```bash
HopCoderX upgrade [target]
```

To upgrade to the latest version.

```bash
HopCoderX upgrade
```

To upgrade to a specific version.

```bash
HopCoderX upgrade v0.1.48
```

#### Flags

| Flag       | Short | Description                                                       |
| ---------- | ----- | ----------------------------------------------------------------- |
| `--method` | `-m`  | The installation method that was used; curl, npm, pnpm, bun, brew |

---

## Global Flags

The HopCoderX CLI takes the following global flags.

| Flag           | Short | Description                          |
| -------------- | ----- | ------------------------------------ |
| `--help`       | `-h`  | Display help                         |
| `--version`    | `-v`  | Print version number                 |
| `--print-logs` |       | Print logs to stderr                 |
| `--log-level`  |       | Log level (DEBUG, INFO, WARN, ERROR) |

---

## Environment variables

HopCoderX can be configured using environment variables.

| Variable                              | Type    | Description                                       |
| ------------------------------------- | ------- | ------------------------------------------------- |
| `HOPCODERX_AUTO_SHARE`                 | boolean | Automatically share sessions                      |
| `HOPCODERX_GIT_BASH_PATH`              | string  | Path to Git Bash executable on Windows            |
| `HOPCODERX_CONFIG`                     | string  | Path to config file                               |
| `HOPCODERX_CONFIG_DIR`                 | string  | Path to config directory                          |
| `HOPCODERX_CONFIG_CONTENT`             | string  | Inline json config content                        |
| `HOPCODERX_DISABLE_AUTOUPDATE`         | boolean | Disable automatic update checks                   |
| `HOPCODERX_DISABLE_PRUNE`              | boolean | Disable pruning of old data                       |
| `HOPCODERX_DISABLE_TERMINAL_TITLE`     | boolean | Disable automatic terminal title updates          |
| `HOPCODERX_PERMISSION`                 | string  | Inlined json permissions config                   |
| `HOPCODERX_DISABLE_DEFAULT_PLUGINS`    | boolean | Disable default plugins                           |
| `HOPCODERX_DISABLE_LSP_DOWNLOAD`       | boolean | Disable automatic LSP server downloads            |
| `HOPCODERX_ENABLE_EXPERIMENTAL_MODELS` | boolean | Enable experimental models                        |
| `HOPCODERX_DISABLE_AUTOCOMPACT`        | boolean | Disable automatic context compaction              |
| `HOPCODERX_DISABLE_CLAUDE_CODE`        | boolean | Disable reading from `.claude` (prompt + skills)  |
| `HOPCODERX_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Disable reading `~/.claude/CLAUDE.md`             |
| `HOPCODERX_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Disable loading `.claude/skills`                  |
| `HOPCODERX_DISABLE_MODELS_FETCH`       | boolean | Disable fetching models from remote sources       |
| `HOPCODERX_FAKE_VCS`                   | string  | Fake VCS provider for testing purposes            |
| `HOPCODERX_DISABLE_FILETIME_CHECK`     | boolean | Disable file time checking for optimization       |
| `HOPCODERX_CLIENT`                     | string  | Client identifier (defaults to `cli`)             |
| `HOPCODERX_ENABLE_EXA`                 | boolean | Enable Exa web search tools                       |
| `HOPCODERX_SERVER_PASSWORD`            | string  | Enable basic auth for `serve`/`web`               |
| `HOPCODERX_SERVER_USERNAME`            | string  | Override basic auth username (default `HopCoderX`) |
| `HOPCODERX_MODELS_URL`                 | string  | Custom URL for fetching models configuration      |

---

### Experimental

These environment variables enable experimental features that may change or be removed.

| Variable                                        | Type    | Description                             |
| ----------------------------------------------- | ------- | --------------------------------------- |
| `HOPCODERX_EXPERIMENTAL`                         | boolean | Enable all experimental features        |
| `HOPCODERX_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Enable icon discovery                   |
| `HOPCODERX_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Disable copy on select in TUI           |
| `HOPCODERX_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Default timeout for bash commands in ms |
| `HOPCODERX_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Max output tokens for LLM responses     |
| `HOPCODERX_EXPERIMENTAL_FILEWATCHER`             | boolean | Enable file watcher for entire dir      |
| `HOPCODERX_EXPERIMENTAL_OXFMT`                   | boolean | Enable oxfmt formatter                  |
| `HOPCODERX_EXPERIMENTAL_LSP_TOOL`                | boolean | Enable experimental LSP tool            |
| `HOPCODERX_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Disable file watcher                    |
| `HOPCODERX_EXPERIMENTAL_EXA`                     | boolean | Enable experimental Exa features        |
| `HOPCODERX_EXPERIMENTAL_LSP_TY`                  | boolean | Enable experimental LSP type checking   |
| `HOPCODERX_EXPERIMENTAL_MARKDOWN`                | boolean | Enable experimental markdown features   |
| `HOPCODERX_EXPERIMENTAL_PLAN_MODE`               | boolean | Enable plan mode                        |
